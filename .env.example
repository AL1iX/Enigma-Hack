# Database (used when running backend locally without Docker)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=support_db
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/support_db

# Frontend dev: API base URL (e.g. http://localhost:8000 when running backend locally)
# VITE_API_URL=http://localhost:8000
